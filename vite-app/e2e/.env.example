# Copy to e2e/.env (gitignored) and fill in. Use seed/test accounts,
# never a real person's sign-in. The second account powers the
# cross-technician race tests; without it those tests skip.
E2E_EMAIL=aaron.toews@seed.vagabonde.ca
E2E_PASSWORD=
E2E_EMAIL2=ben.sawatzky@seed.vagabonde.ca
E2E_PASSWORD2=
